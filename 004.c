#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define CNT 10

// 겹치지 않는 랜덤 숫자를 만들어야 한다. 
int main(){

    int index[CNT];     // 배열값?

    srand(time(0));

    // 0에서 10까지의 겹치지 않는 랜덤 숫자를 출격하는 프로그램을 작성해라. 

    
    // 어떻게 겹치지 않게 만드나?
    // 랜덤 숫자가 생성되면 해당하는 인덱스의 배열 값을 확인해서 
    return 0;
}